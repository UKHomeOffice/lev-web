apiVersion: v1
kind: Service
metadata:
  name: lev-web-dev
  labels:
    name: lev-web-dev
    role: service
  namespace: lev-web-dev
spec:
  type: NodePort
  ports:
  - name: http
    port: 8001
    nodePort: 30200
  selector:
    name: lev-web-dev
