'use strict';

class ResultsPage {

  /**
   * Check results page is visible
   */
  static shouldBeVisible() {
    // Has title
    // this doesn't exist - need to re-write
    // cy.get('h1').contains('records found for');
  }
}

module.exports = ResultsPage;
